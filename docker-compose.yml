services:
  game-of-life:
    build: .
    container_name: gol_webui
    ports:
      - "8088:80"
    restart: unless-stopped
    # Development: use docker compose -f docker-compose.yml -f docker-compose.override.yml up
    # Production: use docker compose up (no volume mount)
